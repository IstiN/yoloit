# Kanban board commands
commands:
  - id: kanban:columns
    human:
      ru:
        - "покажи колонки канбана"
        - "какие колонки есть"
        - "список колонок"
        - "покажи столбцы канбана"
        - "какие колонки на канбане"
        - "можно список колонок?"
        - "пожалуйста покажи колонки"
        - "мне нужны колонки канбана"
        - "все колонки"
        - "открой список колонок"
        - "kanban columns"
      en:
        - "show kanban columns"
        - "list columns"
        - "what columns are there"
        - "show board columns"
        - "which kanban columns exist"
        - "can i see the columns?"
        - "please show the columns"
        - "i need the kanban columns"
        - "all columns"
        - "open column list"
        - "kanban columns"

  - id: kanban:add-column
    human:
      ru:
        - "добавь колонку {name}"
        - "создай колонку {name}"
        - "новая колонка {name}"
        - "сделай колонку {name}"
        - "можно создать колонку {name}?"
        - "пожалуйста добавь колонку {name}"
        - "мне нужна колонка {name}"
        - "организуй колонку {name}"
        - "создать столбец {name}"
        - "хочу колонку {name}"
        - "add column {name}"
      en:
        - "add column {name}"
        - "create column {name}"
        - "new column {name}"
        - "make column {name}"
        - "can you create column {name}?"
        - "please add column {name}"
        - "i need a column {name}"
        - "set up column {name}"
        - "create kanban column {name}"
        - "start column {name}"
        - "add a column called {name}"

  - id: kanban:rename-column
    human:
      ru:
        - "переименуй колонку {column} в {new_name}"
        - "назови колонку {column} {new_name}"
        - "смени имя колонки {column} на {new_name}"
        - "измени название {column} на {new_name}"
        - "можно переименовать колонку {column} в {new_name}?"
        - "пожалуйста переименуй {column} в {new_name}"
        - "хочу назвать колонку {column} как {new_name}"
        - "задай имя {new_name} для колонки {column}"
        - "rename column {column} to {new_name}"
        - "сделай {new_name} названием колонки {column}"
        - "переименовать столбец {column} в {new_name}"
      en:
        - "rename column {column} to {new_name}"
        - "change column name {column} to {new_name}"
        - "rename {column} to {new_name}"
        - "set column {column} name to {new_name}"
        - "can you rename column {column} to {new_name}?"
        - "please rename {column} to {new_name}"
        - "i want column {column} named {new_name}"
        - "update column title {column} to {new_name}"
        - "make column {column} called {new_name}"
        - "change the title of {column} to {new_name}"
        - "rename kanban column {column} to {new_name}"

  - id: kanban:remove-column
    human:
      ru:
        - "удали колонку {column}"
        - "убери колонку {column}"
        - "снеси колонку {column}"
        - "удалить столбец {column}"
        - "можно удалить колонку {column}?"
        - "пожалуйста убери {column}"
        - "мне не нужна колонка {column}"
        - "delete column {column}"
        - "выкинь колонку {column}"
        - "удали канбан-колонку {column}"
        - "remove column {column}"
      en:
        - "remove column {column}"
        - "delete column {column}"
        - "drop column {column}"
        - "remove kanban column {column}"
        - "can you delete column {column}?"
        - "please remove {column}"
        - "i don't need column {column} anymore"
        - "get rid of column {column}"
        - "erase column {column}"
        - "trash column {column}"
        - "delete the column {column}"

  - id: kanban:add-card
    human:
      ru:
        - "добавь карточку {title} в {column}"
        - "создай карточку {title} в {column}"
        - "новая карточка {title} в {column}"
        - "добавь задачу {title} в {column}"
        - "добавь в {column} {title}"
        - "сделай карточку {title} в {column}"
        - "можно добавить карточку {title} в {column}?"
        - "пожалуйста создай карточку {title} в {column}"
        - "мне нужна карточка {title} в {column}"
        - "закинь {title} в колонку {column}"
        - "add card {title} to {column}"
      en:
        - "add card {title} to {column}"
        - "create card {title} in {column}"
        - "new card {title} in {column}"
        - "add task {title} to {column}"
        - "add {title} to {column}"
        - "make card {title} in {column}"
        - "can you add card {title} to {column}?"
        - "please create card {title} in {column}"
        - "i need a card {title} in {column}"
        - "put card {title} into {column}"
        - "start card {title} in {column}"

  - id: kanban:move-card
    human:
      ru:
        - "перемести карточку {card_id} в {to_column}"
        - "двинь {card_id} в {to_column}"
        - "передвинь {card_id} в {to_column}"
        - "перетащи {card_id} в {to_column}"
        - "можно перенести карточку {card_id} в {to_column}?"
        - "пожалуйста перемести {card_id} в {to_column}"
        - "мне нужно отправить {card_id} в {to_column}"
        - "move card {card_id} to {to_column}"
        - "скинь {card_id} в колонку {to_column}"
        - "переложи {card_id} в {to_column}"
        - "перекинь карточку {card_id} в {to_column}"
      en:
        - "move card {card_id} to {to_column}"
        - "move {card_id} to {to_column}"
        - "drag {card_id} to {to_column}"
        - "send card {card_id} to {to_column}"
        - "can you move {card_id} to {to_column}?"
        - "please move card {card_id} to {to_column}"
        - "i need {card_id} in {to_column}"
        - "shift card {card_id} to {to_column}"
        - "drop {card_id} into {to_column}"
        - "transfer card {card_id} to {to_column}"
        - "move the card {card_id} to {to_column}"

  - id: kanban:remove-card
    human:
      ru:
        - "удали карточку {card_id}"
        - "убери карточку {card_id}"
        - "удали задачу {card_id}"
        - "снеси карточку {card_id}"
        - "можно удалить карточку {card_id}?"
        - "пожалуйста убери {card_id}"
        - "мне не нужна карточка {card_id}"
        - "delete card {card_id}"
        - "выкинь карточку {card_id}"
        - "удали канбан-карточку {card_id}"
        - "remove task {card_id}"
      en:
        - "remove card {card_id}"
        - "delete card {card_id}"
        - "remove task {card_id}"
        - "drop card {card_id}"
        - "can you delete card {card_id}?"
        - "please remove {card_id}"
        - "i don't need card {card_id} anymore"
        - "get rid of card {card_id}"
        - "erase card {card_id}"
        - "trash card {card_id}"
        - "delete the card {card_id}"

  - id: kanban:update-card
    human:
      ru:
        - "обнови карточку {card_id} на {new_title}"
        - "переименуй карточку {card_id} в {new_title}"
        - "измени карточку {card_id} на {new_title}"
        - "смени заголовок карточки {card_id} на {new_title}"
        - "можно обновить {card_id} на {new_title}?"
        - "пожалуйста переименуй {card_id} в {new_title}"
        - "мне нужно назвать {card_id} как {new_title}"
        - "update card {card_id} to {new_title}"
        - "сделай название {new_title} у карточки {card_id}"
        - "задай новый текст {new_title} для {card_id}"
        - "rename card {card_id} to {new_title}"
      en:
        - "update card {card_id} to {new_title}"
        - "rename card {card_id} to {new_title}"
        - "change card {card_id} to {new_title}"
        - "change the title of {card_id} to {new_title}"
        - "can you update {card_id} to {new_title}?"
        - "please rename {card_id} to {new_title}"
        - "i need card {card_id} named {new_title}"
        - "set card {card_id} title to {new_title}"
        - "make {new_title} the new title for {card_id}"
        - "edit card {card_id} to {new_title}"
        - "update the card {card_id} to {new_title}"

  - id: kanban:cards
    human:
      ru:
        - "покажи карточки"
        - "список карточек"
        - "все карточки"
        - "покажи все карточки"
        - "какие карточки есть"
        - "можно список карточек?"
        - "пожалуйста покажи карточки"
        - "мне нужны карточки"
        - "карточки канбана"
        - "открой список карточек"
        - "show cards"
      en:
        - "show cards"
        - "list cards"
        - "all cards"
        - "show all cards"
        - "what cards are there"
        - "can i see the cards?"
        - "please show the cards"
        - "i need the card list"
        - "kanban cards"
        - "open card list"
        - "display cards"
