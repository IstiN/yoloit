# Link, timer, playlist, webpage, run, yolochat, models commands
commands:
  # ── Links ──
  - id: links
    human:
      ru:
        - "покажи связи"
        - "список связей"
        - "какие связи есть"
        - "покажи все связи"
        - "можно список связей?"
        - "какие линki есть"
        - "мне нужны связи"
        - "открой связи"
        - "связи на борде"
        - "линки"
        - "show links"
      en:
        - "show links"
        - "list links"
        - "what links are there"
        - "show all links"
        - "can i see the links?"
        - "which links exist"
        - "i need the links"
        - "open links"
        - "board links"
        - "display links"
        - "links"

  - id: link:create
    human:
      ru:
        - "свяжи {from} с {to}"
        - "создай связь от {from} к {to}"
        - "соедини {from} и {to}"
        - "сделай линк от {from} к {to}"
        - "можно связать {from} с {to}?"
        - "пожалуйста свяжи {from} и {to}"
        - "мне нужна связь между {from} и {to}"
        - "протяни связь от {from} к {to}"
        - "линкань {from} к {to}"
        - "link {from} to {to}"
        - "соедини узлы {from} и {to}"
      en:
        - "link {from} to {to}"
        - "connect {from} to {to}"
        - "create link from {from} to {to}"
        - "join {from} and {to}"
        - "can you link {from} to {to}?"
        - "please connect {from} with {to}"
        - "i need a link from {from} to {to}"
        - "draw a link from {from} to {to}"
        - "wire {from} to {to}"
        - "attach {from} to {to}"
        - "make a connection from {from} to {to}"

  - id: link:delete
    human:
      ru:
        - "удали связь {link_id}"
        - "убери связь {link_id}"
        - "удали линк {link_id}"
        - "снеси связь {link_id}"
        - "можно удалить связь {link_id}?"
        - "пожалуйста убери линк {link_id}"
        - "мне не нужна связь {link_id}"
        - "delete link {link_id}"
        - "выкинь связь {link_id}"
        - "разорви связь {link_id}"
        - "remove link {link_id}"
      en:
        - "delete link {link_id}"
        - "remove link {link_id}"
        - "delete connection {link_id}"
        - "drop link {link_id}"
        - "can you delete link {link_id}?"
        - "please remove link {link_id}"
        - "i don't need link {link_id} anymore"
        - "erase link {link_id}"
        - "unlink {link_id}"
        - "get rid of link {link_id}"
        - "remove connection {link_id}"

  - id: link:style
    human:
      ru:
        - "стиль связи {link_id} {style} {geometry}"
        - "измени стиль связи {link_id}"
        - "задай стиль {style} {geometry} для связи {link_id}"
        - "сделай связь {link_id} стилем {style} {geometry}"
        - "можно поменять стиль связи {link_id}?"
        - "пожалуйста смени стиль {link_id} на {style} {geometry}"
        - "мне нужен стиль {style} {geometry} для {link_id}"
        - "оформи связь {link_id} как {style} {geometry}"
        - "link style {link_id} {style} {geometry}"
        - "перестрой связь {link_id} в {style} {geometry}"
        - "измени геометрию связи {link_id} на {style} {geometry}"
      en:
        - "set link style {link_id} {style} {geometry}"
        - "change link style {link_id}"
        - "set style of link {link_id} to {style} {geometry}"
        - "make link {link_id} {style} {geometry}"
        - "can you change link {link_id} style?"
        - "please set link {link_id} to {style} {geometry}"
        - "i need link {link_id} styled {style} {geometry}"
        - "restyle link {link_id} {style} {geometry}"
        - "format link {link_id} as {style} {geometry}"
        - "update link {link_id} style to {style} {geometry}"
        - "apply {style} {geometry} to link {link_id}"

  - id: link:color
    human:
      ru:
        - "цвет связи {link_id} {color}"
        - "покрась связь {link_id} в {color}"
        - "задай цвет {color} для связи {link_id}"
        - "сделай связь {link_id} цвета {color}"
        - "можно поменять цвет связи {link_id} на {color}?"
        - "пожалуйста покрась линк {link_id} в {color}"
        - "мне нужен цвет {color} для связи {link_id}"
        - "раскрась связь {link_id} в {color}"
        - "link color {link_id} {color}"
        - "сделай линк {link_id} {color}"
        - "измени цвет связи {link_id} на {color}"
      en:
        - "link color {link_id} {color}"
        - "color link {link_id} {color}"
        - "set link {link_id} color to {color}"
        - "paint link {link_id} {color}"
        - "can you color link {link_id} {color}?"
        - "please set link {link_id} to {color}"
        - "i need link {link_id} in {color}"
        - "recolor link {link_id} {color}"
        - "change the color of link {link_id} to {color}"
        - "make link {link_id} {color}"
        - "set connection {link_id} color to {color}"

  # ── Timer ──
  - id: timer:create
    human:
      ru:
        - "поставь таймер на {duration} секунд"
        - "таймер {duration}"
        - "засеки {duration}"
        - "создай таймер {label}"
        - "таймер на {duration} минут"
        - "поставь таймер"
        - "запусти таймер на {duration}"
        - "можно таймер на {duration}?"
        - "пожалуйста создай таймер {label}"
        - "мне нужен таймер {label}"
        - "старт таймер {duration}"
      en:
        - "set timer for {duration} seconds"
        - "timer {duration}"
        - "start a timer {label}"
        - "create timer {label}"
        - "set timer for {duration} minutes"
        - "start timer"
        - "run a timer for {duration}"
        - "can you set a timer for {duration}?"
        - "please create timer {label}"
        - "i need a timer {label}"
        - "begin timer {duration}"

  # ── Playlist ──
  - id: play
    human:
      ru:
        - "включи {file}"
        - "проиграй {file}"
        - "воспроизведи {file}"
        - "плей {file}"
        - "запусти {file}"
        - "можно включить {file}?"
        - "пожалуйста воспроизведи {file}"
        - "мне нужно проиграть {file}"
        - "стартуй медиа {file}"
        - "play {file}"
        - "открой плеер для {file}"
      en:
        - "play {file}"
        - "start playing {file}"
        - "play media {file}"
        - "open {file}"
        - "can you play {file}?"
        - "please play {file}"
        - "i want to hear {file}"
        - "start {file}"
        - "launch media {file}"
        - "play this file {file}"
        - "begin playback for {file}"

  # ── Webpage ──
  - id: web:open
    human:
      ru:
        - "открой сайт {url}"
        - "открой ссылку {url}"
        - "загрузи {url}"
        - "покажи страницу {url}"
        - "открой url {url}"
        - "можно открыть {url}?"
        - "пожалуйста открой страницу {url}"
        - "мне нужен сайт {url}"
        - "перейди на {url}"
        - "web open {url}"
        - "зайди на {url}"
      en:
        - "open website {url}"
        - "open url {url}"
        - "go to {url}"
        - "load page {url}"
        - "open webpage {url}"
        - "can you open {url}?"
        - "please open {url}"
        - "i need site {url}"
        - "navigate to {url}"
        - "visit {url}"
        - "load website {url}"

  # ── Run sessions ──
  - id: run:list
    human:
      ru:
        - "покажи запущенные сессии"
        - "список сессий"
        - "что запущено"
        - "какие сессии активны"
        - "можно список ран-сессий?"
        - "пожалуйста покажи сессии"
        - "мне нужны активные сессии"
        - "открой список запусков"
        - "run sessions"
        - "покажи рантаймы"
        - "все сессии"
      en:
        - "show running sessions"
        - "list sessions"
        - "what is running"
        - "which sessions are active"
        - "can i see the run sessions?"
        - "please show sessions"
        - "i need active sessions"
        - "open run list"
        - "run sessions"
        - "show runtimes"
        - "all sessions"

  - id: run:input
    human:
      ru:
        - "введи {text} в сессию {session}"
        - "отправь {text} в {session}"
        - "передай {text} в сессию {session}"
        - "впиши {text} в {session}"
        - "можно отправить {text} в {session}?"
        - "пожалуйста введи {text} в {session}"
        - "мне нужно послать {text} в {session}"
        - "send {text} to session {session}"
        - "пробрось {text} в {session}"
        - "отправь ввод {text} в {session}"
        - "засунь {text} в {session}"
      en:
        - "send {text} to session {session}"
        - "input {text} to {session}"
        - "type {text} into session {session}"
        - "pass {text} to {session}"
        - "can you send {text} to {session}?"
        - "please input {text} into {session}"
        - "i need to send {text} to {session}"
        - "write {text} to session {session}"
        - "feed {text} into {session}"
        - "enter {text} in {session}"
        - "push {text} to {session}"

  - id: run:attach
    human:
      ru:
        - "подключись к сессии {session}"
        - "аттач {session}"
        - "присоединись к {session}"
        - "открой сессию {session}"
        - "можно подключиться к {session}?"
        - "пожалуйста аттачнись к {session}"
        - "мне нужен доступ к {session}"
        - "attach to session {session}"
        - "зайди в сессию {session}"
        - "подцепись к {session}"
        - "подключи меня к {session}"
      en:
        - "attach to session {session}"
        - "connect to {session}"
        - "join session {session}"
        - "open session {session}"
        - "can you attach to {session}?"
        - "please connect me to {session}"
        - "i need access to {session}"
        - "attach {session}"
        - "hook into session {session}"
        - "enter session {session}"
        - "connect me with {session}"

  - id: run:detach
    human:
      ru:
        - "отключись от сессии {session}"
        - "детач {session}"
        - "отсоединись от {session}"
        - "выйди из сессии {session}"
        - "можно отключиться от {session}?"
        - "пожалуйста детачнись от {session}"
        - "мне нужно отсоединиться от {session}"
        - "detach session {session}"
        - "отцепись от {session}"
        - "закрой подключение к {session}"
        - "disconnect {session}"
      en:
        - "detach session {session}"
        - "disconnect {session}"
        - "detach from session {session}"
        - "leave session {session}"
        - "can you detach from {session}?"
        - "please disconnect from {session}"
        - "i need to leave {session}"
        - "detach {session}"
        - "drop connection to {session}"
        - "exit session {session}"
        - "unhook from {session}"

  - id: run:output
    human:
      ru:
        - "покажи вывод сессии {session}"
        - "лог сессии {session}"
        - "покажи output {session}"
        - "что вывела сессия {session}"
        - "можно посмотреть вывод {session}?"
        - "пожалуйста открой лог {session}"
        - "мне нужен вывод сессии {session}"
        - "show output of {session}"
        - "покажи консоль {session}"
        - "журнал {session}"
        - "читай вывод {session}"
      en:
        - "show output of {session}"
        - "session output {session}"
        - "show logs for {session}"
        - "what did session {session} print"
        - "can i see output from {session}?"
        - "please open output for {session}"
        - "i need the output of {session}"
        - "display console for {session}"
        - "read output {session}"
        - "show session log {session}"
        - "open output stream {session}"

  - id: run:popout
    human:
      ru:
        - "открой сессию {session} в панели"
        - "попаут {session}"
        - "вынеси сессию {session} в отдельную панель"
        - "открой попаут для {session}"
        - "можно вынести {session} в панель?"
        - "пожалуйста попауть {session}"
        - "мне нужна отдельная панель для {session}"
        - "popout session {session}"
        - "всплывающее окно для {session}"
        - "открой {session} отдельно"
        - "покажи {session} в новом окне"
      en:
        - "popout session {session}"
        - "open {session} in panel"
        - "open session {session} in a panel"
        - "pop out {session}"
        - "can you popout {session}?"
        - "please open {session} separately"
        - "i need a separate panel for {session}"
        - "show {session} in a new panel"
        - "detach {session} into panel"
        - "open popout for {session}"
        - "move session {session} to panel"

  # ── YoloChat ──
  - id: yolochat:panels
    human:
      ru:
        - "покажи чат панели"
        - "список чатов"
        - "какие чаты есть"
        - "покажи все чаты"
        - "можно список чат-панелей?"
        - "пожалуйста покажи чаты"
        - "мне нужны чат панели"
        - "открой список чатов"
        - "yolochat панели"
        - "чат панели"
        - "show chat panels"
      en:
        - "show chat panels"
        - "list chats"
        - "what chats are there"
        - "show all chats"
        - "can i see the chat panels?"
        - "please show chats"
        - "i need the chat panels"
        - "open chat list"
        - "yolochat panels"
        - "chat panels"
        - "display chats"

  - id: yolochat:send
    human:
      ru:
        - "отправь сообщение {text}"
        - "напиши в чат {text}"
        - "скажи {text}"
        - "пошли в чат {text}"
        - "можно отправить {text}?"
        - "пожалуйста напиши {text}"
        - "мне нужно отправить {text} в чат"
        - "send message {text}"
        - "зашли {text}"
        - "отправь текст {text}"
        - "передай в чат {text}"
      en:
        - "send message {text}"
        - "say {text}"
        - "write {text} in chat"
        - "send {text} to chat"
        - "can you send {text}?"
        - "please write {text}"
        - "i need to send {text} to chat"
        - "post {text} in chat"
        - "message the chat: {text}"
        - "submit {text}"
        - "send chat text {text}"

  - id: yolochat:messages
    human:
      ru:
        - "покажи сообщения чата"
        - "история чата"
        - "прочитай сообщения"
        - "покажи чат-историю"
        - "можно посмотреть сообщения?"
        - "пожалуйста открой историю чата"
        - "мне нужны сообщения чата"
        - "show chat messages"
        - "что писали в чате"
        - "открой переписку"
        - "все сообщения"
      en:
        - "show chat messages"
        - "chat history"
        - "read messages"
        - "show conversation history"
        - "can i see the chat messages?"
        - "please open chat history"
        - "i need the chat messages"
        - "display the conversation"
        - "what was said in chat"
        - "open the messages"
        - "all chat messages"

  # ── Models ──
  - id: models:list
    human:
      ru:
        - "покажи модели"
        - "список моделей"
        - "какие модели есть"
        - "локальные модели"
        - "покажи все модели"
        - "можно список моделей?"
        - "пожалуйста покажи модели"
        - "мне нужны доступные модели"
        - "модельки"
        - "какие llm доступны"
        - "show models"
      en:
        - "show models"
        - "list models"
        - "what models are available"
        - "local models"
        - "show all models"
        - "can i see the models?"
        - "please show models"
        - "i need available models"
        - "which llms are available"
        - "display models"
        - "models"

  - id: models:download
    human:
      ru:
        - "скачай модель {model_id}"
        - "загрузи модель {model_id}"
        - "установи модель {model_id}"
        - "подтяни модель {model_id}"
        - "можно скачать модель {model_id}?"
        - "пожалуйста загрузи {model_id}"
        - "мне нужна модель {model_id}"
        - "download model {model_id}"
        - "докачай модель {model_id}"
        - "получи модель {model_id}"
        - "импортируй модель {model_id}"
      en:
        - "download model {model_id}"
        - "install model {model_id}"
        - "get model {model_id}"
        - "fetch model {model_id}"
        - "can you download model {model_id}?"
        - "please install {model_id}"
        - "i need model {model_id}"
        - "pull model {model_id}"
        - "grab model {model_id}"
        - "download the model {model_id}"
        - "bring in model {model_id}"

  - id: models:delete
    human:
      ru:
        - "удали модель {model_id}"
        - "убери модель {model_id}"
        - "снеси модель {model_id}"
        - "можно удалить модель {model_id}?"
        - "пожалуйста убери {model_id}"
        - "мне не нужна модель {model_id}"
        - "delete model {model_id}"
        - "выкинь модель {model_id}"
        - "деинсталь модель {model_id}"
        - "remove model {model_id}"
        - "стереть модель {model_id}"
      en:
        - "delete model {model_id}"
        - "remove model {model_id}"
        - "delete the model {model_id}"
        - "drop model {model_id}"
        - "can you delete model {model_id}?"
        - "please remove {model_id}"
        - "i don't need model {model_id} anymore"
        - "erase model {model_id}"
        - "uninstall model {model_id}"
        - "get rid of model {model_id}"
        - "trash model {model_id}"

  - id: models:select
    human:
      ru:
        - "выбери модель {model_id} для {kind}"
        - "используй модель {model_id}"
        - "установи модель {model_id} для чата"
        - "сделай {model_id} активной моделью для {kind}"
        - "можно выбрать модель {model_id} для {kind}?"
        - "пожалуйста используй {model_id} для {kind}"
        - "мне нужна модель {model_id} для {kind}"
        - "select model {model_id} for {kind}"
        - "переключи {kind} на модель {model_id}"
        - "назначь {model_id} для {kind}"
        - "сделай модель {model_id} основной"
      en:
        - "select model {model_id} for {kind}"
        - "use model {model_id}"
        - "set model {model_id} for chat"
        - "make {model_id} the active model for {kind}"
        - "can you select model {model_id} for {kind}?"
        - "please use {model_id} for {kind}"
        - "i need model {model_id} for {kind}"
        - "switch {kind} to model {model_id}"
        - "assign {model_id} to {kind}"
        - "set active model {model_id} for {kind}"
        - "make {model_id} the default model"

  - id: models:pause
    human:
      ru:
        - "пауза загрузки {model_id}"
        - "приостанови загрузку {model_id}"
        - "поставь загрузку {model_id} на паузу"
        - "можно приостановить {model_id}?"
        - "пожалуйста поставь на паузу {model_id}"
        - "мне нужно заморозить загрузку {model_id}"
        - "pause download {model_id}"
        - "замри загрузка {model_id}"
        - "стопни пока загрузку {model_id}"
        - "пауза {model_id}"
        - "прерви загрузку {model_id} временно"
      en:
        - "pause download {model_id}"
        - "stop downloading {model_id}"
        - "pause model download {model_id}"
        - "can you pause {model_id}?"
        - "please pause download of {model_id}"
        - "i need to pause {model_id}"
        - "hold download {model_id}"
        - "freeze download {model_id}"
        - "put {model_id} on pause"
        - "pause {model_id}"
        - "temporarily stop {model_id} download"

  - id: models:resume
    human:
      ru:
        - "продолжи загрузку {model_id}"
        - "возобнови загрузку {model_id}"
        - "сними с паузы {model_id}"
        - "можно продолжить загрузку {model_id}?"
        - "пожалуйста возобнови {model_id}"
        - "мне нужно докачать {model_id}"
        - "resume download {model_id}"
        - "продолжай качать {model_id}"
        - "разморозь загрузку {model_id}"
        - "верни загрузку {model_id}"
        - "возобновить модель {model_id}"
      en:
        - "resume download {model_id}"
        - "continue downloading {model_id}"
        - "resume model {model_id}"
        - "can you resume {model_id}?"
        - "please continue download of {model_id}"
        - "i need to finish downloading {model_id}"
        - "restart download {model_id}"
        - "unpause {model_id}"
        - "pick up download {model_id} again"
        - "continue with {model_id}"
        - "resume {model_id} download"

  - id: models:cancel
    human:
      ru:
        - "отмени загрузку {model_id}"
        - "отменить загрузку {model_id}"
        - "кансел загрузку {model_id}"
        - "сбрось загрузку {model_id}"
        - "можно отменить {model_id}?"
        - "пожалуйста отмени загрузку модели {model_id}"
        - "мне нужно прервать загрузку {model_id}"
        - "cancel download {model_id}"
        - "отруби загрузку {model_id}"
        - "аннулируй скачивание {model_id}"
        - "убери загрузку {model_id}"
      en:
        - "cancel download {model_id}"
        - "cancel model download {model_id}"
        - "abort download {model_id}"
        - "can you cancel {model_id}?"
        - "please cancel download of {model_id}"
        - "i need to cancel {model_id}"
        - "stop and cancel {model_id}"
        - "drop download {model_id}"
        - "terminate download {model_id}"
        - "cancel {model_id} download"
        - "abort the download of {model_id}"

  - id: models:stop
    human:
      ru:
        - "останови загрузку {model_id}"
        - "стоп загрузки {model_id}"
        - "останови скачивание {model_id}"
        - "можно остановить {model_id}?"
        - "пожалуйста стопни загрузку {model_id}"
        - "мне нужно остановить {model_id}"
        - "stop download {model_id}"
        - "прекрати загрузку {model_id}"
        - "стопни модель {model_id}"
        - "замри загрузка модели {model_id}"
        - "хватит качать {model_id}"
      en:
        - "stop download {model_id}"
        - "stop downloading {model_id}"
        - "halt download {model_id}"
        - "can you stop {model_id}?"
        - "please stop download of {model_id}"
        - "i need to stop {model_id}"
        - "end download {model_id}"
        - "freeze download {model_id}"
        - "stop model {model_id} download"
        - "cut off download {model_id}"
        - "stop the download of {model_id}"

  - id: lm:generate
    human:
      ru:
        - "сгенерируй {message}"
        - "спроси модель {message}"
        - "сделай генерацию {message}"
        - "ответь на {message}"
        - "можно сгенерировать {message}?"
        - "пожалуйста сгенерируй {message}"
        - "мне нужен ответ на {message}"
        - "generate {message}"
        - "дай ответ по {message}"
        - "попроси модель: {message}"
        - "создай текст {message}"
      en:
        - "generate {message}"
        - "ask model {message}"
        - "generate text for {message}"
        - "answer {message}"
        - "can you generate {message}?"
        - "please generate {message}"
        - "i need a response for {message}"
        - "prompt the model with {message}"
        - "make text for {message}"
        - "produce output for {message}"
        - "run generation for {message}"
