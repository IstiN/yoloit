# App/widget commands
commands:
  - id: reload
    human:
      ru:
        - "перезагрузи"
        - "хот релоад"
        - "обнови"
        - "релоад"
        - "сделай релоад"
        - "перезагрузи приложение"
        - "обнови сейчас"
        - "можно перезагрузить?"
        - "пожалуйста перезагрузи"
        - "мне нужен релоад"
        - "давай обновим"
      en:
        - "reload"
        - "hot reload"
        - "refresh"
        - "do a reload"
        - "reload the app"
        - "can you reload?"
        - "please reload"
        - "i need a reload"
        - "refresh it"
        - "run hot reload"
        - "do a quick refresh"

  - id: restart
    human:
      ru:
        - "перезапусти"
        - "рестарт"
        - "хот рестарт"
        - "сделай рестарт"
        - "перезапусти приложение"
        - "можно рестарт?"
        - "пожалуйста перезапусти"
        - "нужен перезапуск"
        - "давай перезапустим"
        - "выполни рестарт"
        - "сделай hot restart"
      en:
        - "restart"
        - "hot restart"
        - "restart the app"
        - "do a restart"
        - "can you restart?"
        - "please restart"
        - "i need a restart"
        - "run a hot restart"
        - "restart it"
        - "start over"
        - "do a full restart"

  - id: help
    human:
      ru:
        - "помощь"
        - "что ты умеешь"
        - "какие команды есть"
        - "покажи команды"
        - "хелп"
        - "покажи помощь"
        - "можно список команд?"
        - "что можно сделать"
        - "мне нужна помощь"
        - "подскажи команды"
        - "плиз хелп"
      en:
        - "help"
        - "what can you do"
        - "show commands"
        - "list commands"
        - "show help"
        - "can you help?"
        - "i need help"
        - "what commands are available"
        - "please show help"
        - "how do i use this?"
        - "help me"

  - id: app:list
    human:
      ru:
        - "покажи приложения"
        - "список приложений"
        - "мои приложения"
        - "какие приложения есть"
        - "какие аппы установлены"
        - "покажи список аппов"
        - "можно увидеть приложения?"
        - "мне нужен список приложений"
        - "все приложения"
        - "покажи установленные приложения"
        - "аппы"
      en:
        - "show apps"
        - "list apps"
        - "my apps"
        - "what apps are installed"
        - "show installed apps"
        - "can i see the apps?"
        - "i need the app list"
        - "all apps"
        - "which apps do i have"
        - "display apps"
        - "apps"

  - id: app:run
    human:
      ru:
        - "запусти приложение {id_or_path}"
        - "открой приложение {id_or_path}"
        - "запусти {id_or_path}"
        - "открой {id_or_path}"
        - "стартуй приложение {id_or_path}"
        - "можно открыть {id_or_path}?"
        - "пожалуйста запусти {id_or_path}"
        - "мне нужно приложение {id_or_path}"
        - "загрузи приложение {id_or_path}"
        - "launcher {id_or_path}"
        - "включи приложение {id_or_path}"
      en:
        - "run app {id_or_path}"
        - "open app {id_or_path}"
        - "launch {id_or_path}"
        - "start {id_or_path}"
        - "open {id_or_path}"
        - "can you run {id_or_path}?"
        - "please launch {id_or_path}"
        - "i want to open {id_or_path}"
        - "load app {id_or_path}"
        - "start app {id_or_path}"
        - "bring up {id_or_path}"

  - id: app:install
    human:
      ru:
        - "установи приложение {source}"
        - "инсталл {source}"
        - "поставь {source}"
        - "добавь приложение {source}"
        - "можно установить {source}?"
        - "пожалуйста установи {source}"
        - "мне нужно установить {source}"
        - "подключи приложение {source}"
        - "заинсталь {source}"
        - "установи апп {source}"
        - "хочу добавить {source}"
      en:
        - "install app {source}"
        - "install {source}"
        - "add app {source}"
        - "can you install {source}?"
        - "please install {source}"
        - "i need to install {source}"
        - "add {source}"
        - "set up app {source}"
        - "install this app {source}"
        - "get app {source}"
        - "bring in {source}"

  - id: app:remove
    human:
      ru:
        - "удали приложение {id}"
        - "удали апп {id}"
        - "убери приложение {id}"
        - "деинсталл {id}"
        - "снеси приложение {id}"
        - "можно удалить {id}?"
        - "пожалуйста убери {id}"
        - "мне больше не нужно приложение {id}"
        - "удали {id} из приложений"
        - "отключи приложение {id}"
        - "remove app {id}"
      en:
        - "remove app {id}"
        - "uninstall app {id}"
        - "delete app {id}"
        - "remove {id}"
        - "can you uninstall {id}?"
        - "please delete {id}"
        - "i don't need app {id} anymore"
        - "take out app {id}"
        - "drop app {id}"
        - "uninstall {id}"
        - "get rid of app {id}"

  - id: app:create
    human:
      ru:
        - "создай приложение {name}"
        - "новое приложение {name}"
        - "сделай приложение {name}"
        - "создай апп {name}"
        - "можно создать приложение {name}?"
        - "пожалуйста создай приложение {name}"
        - "мне нужно новое приложение {name}"
        - "организуй приложение {name}"
        - "заскаффолди приложение {name}"
        - "хочу приложение {name}"
        - "создать приложение {name}"
      en:
        - "create app {name}"
        - "new app {name}"
        - "scaffold app {name}"
        - "make app {name}"
        - "can you create app {name}?"
        - "please create app {name}"
        - "i need a new app {name}"
        - "generate app {name}"
        - "set up app {name}"
        - "build app {name}"
        - "create an app called {name}"

  - id: app:execute
    human:
      ru:
        - "выполни {action} в приложении {id}"
        - "нажми {action} в {id}"
        - "запусти действие {action} в {id}"
        - "сделай {action} в приложении {id}"
        - "можно выполнить {action} в {id}?"
        - "пожалуйста выполни {action} в {id}"
        - "триггерни {action} в {id}"
        - "активируй {action} в приложении {id}"
        - "вызови {action} в {id}"
        - "мне нужно действие {action} в {id}"
        - "execute {action} in {id}"
      en:
        - "execute {action} in app {id}"
        - "trigger {action} in {id}"
        - "run {action} in app {id}"
        - "do {action} in {id}"
        - "can you execute {action} in {id}?"
        - "please trigger {action} in {id}"
        - "activate {action} in app {id}"
        - "invoke {action} in {id}"
        - "i need {action} in {id}"
        - "perform {action} in {id}"
        - "fire {action} in {id}"

  - id: app:reload
    human:
      ru:
        - "перезагрузи приложение {id}"
        - "обнови приложение {id}"
        - "релоад приложения {id}"
        - "сделай релоад {id}"
        - "можно перезагрузить приложение {id}?"
        - "пожалуйста обнови {id}"
        - "мне нужен релоад приложения {id}"
        - "перезагрузи апп {id}"
        - "обнови {id} сейчас"
        - "reload app {id}"
        - "сделай hot reload для {id}"
      en:
        - "reload app {id}"
        - "refresh app {id}"
        - "reload {id}"
        - "can you reload app {id}?"
        - "please refresh {id}"
        - "i need to reload {id}"
        - "do a reload for {id}"
        - "run hot reload on {id}"
        - "refresh {id} now"
        - "reload the application {id}"
        - "reopen app {id}"

  - id: app:logs
    human:
      ru:
        - "покажи логи приложения {id}"
        - "логи {id}"
        - "выведи логи {id}"
        - "покажи журнал приложения {id}"
        - "можно посмотреть логи {id}?"
        - "пожалуйста открой логи {id}"
        - "мне нужны логи приложения {id}"
        - "что в логах {id}"
        - "лог приложения {id}"
        - "покажи вывод {id}"
        - "logs for {id}"
      en:
        - "show app logs {id}"
        - "app logs {id}"
        - "logs for {id}"
        - "show logs for app {id}"
        - "can i see logs for {id}?"
        - "please open logs for {id}"
        - "i need the logs for {id}"
        - "display app log {id}"
        - "what do the logs say for {id}"
        - "show output for {id}"
        - "open the logs for {id}"

  - id: app:snapshot
    human:
      ru:
        - "снепшот приложения {id}"
        - "состояние приложения {id}"
        - "сделай снимок приложения {id}"
        - "покажи снапшот {id}"
        - "можно snapshot приложения {id}?"
        - "пожалуйста сохрани состояние {id}"
        - "мне нужен снимок приложения {id}"
        - "зафиксируй состояние {id}"
        - "сделай snapshot {id}"
        - "сними состояние приложения {id}"
        - "app snapshot {id}"
      en:
        - "app snapshot {id}"
        - "app state {id}"
        - "take a snapshot of app {id}"
        - "capture app state {id}"
        - "can you snapshot {id}?"
        - "please save the state of {id}"
        - "i need a snapshot of {id}"
        - "record the state of {id}"
        - "grab a snapshot for {id}"
        - "snapshot the app {id}"
        - "save app snapshot {id}"

  - id: app:screenshot
    human:
      ru:
        - "скриншот приложения {id}"
        - "сфоткай приложение {id}"
        - "сделай скрин приложения {id}"
        - "скрин приложения {id}"
        - "можно скриншот {id}?"
        - "пожалуйста сними экран приложения {id}"
        - "мне нужен скрин {id}"
        - "сделай screenshot {id}"
        - "захвати изображение {id}"
        - "сохрани скриншот {id}"
        - "capture app {id}"
      en:
        - "screenshot app {id}"
        - "capture app {id}"
        - "take a screenshot of {id}"
        - "grab a screenshot for {id}"
        - "can you screenshot {id}?"
        - "please capture {id}"
        - "i need a screenshot of {id}"
        - "save a screenshot of {id}"
        - "screen capture app {id}"
        - "take app shot {id}"
        - "snapshot the screen for {id}"

  - id: app:dev-skill
    human:
      ru:
        - "покажи гайд по разработке"
        - "документация по виджетам"
        - "покажи доки для разработки"
        - "как разрабатывать виджеты"
        - "можно гайд для разработчика?"
        - "пожалуйста открой dev guide"
        - "мне нужна документация по приложениям"
        - "дев скилл"
        - "открой руководство по виджетам"
        - "покажи dev docs"
        - "хочу docs по разработке"
      en:
        - "show dev guide"
        - "widget development docs"
        - "open development guide"
        - "how do i build widgets"
        - "can you show the dev docs?"
        - "please open the widget guide"
        - "i need app development docs"
        - "dev skill"
        - "show developer documentation"
        - "open dev docs"
        - "widget dev help"

  - id: app:demo
    human:
      ru:
        - "покажи демо приложения"
        - "примеры приложений"
        - "демо"
        - "покажи демо"
        - "можно посмотреть демо?"
        - "пожалуйста открой демо приложения"
        - "мне нужны примеры"
        - "демо аппов"
        - "покажи sample apps"
        - "хочу увидеть примеры приложений"
        - "витрина приложений"
      en:
        - "show demo apps"
        - "demo apps"
        - "examples"
        - "show demos"
        - "can i see demo apps?"
        - "please open the app demos"
        - "i need examples"
        - "sample apps"
        - "show example applications"
        - "demo showcase"
        - "let me see app examples"

  - id: app:demo-view
    human:
      ru:
        - "покажи код демо {id}"
        - "исходник демо {id}"
        - "открой демо {id}"
        - "покажи исходники {id}"
        - "можно посмотреть код демо {id}?"
        - "пожалуйста открой source демо {id}"
        - "мне нужен код примера {id}"
        - "посмотреть демо {id}"
        - "покажи demo source {id}"
        - "код примера {id}"
        - "где исходник демо {id}"
      en:
        - "show demo source {id}"
        - "view demo {id}"
        - "open demo source {id}"
        - "show code for demo {id}"
        - "can i see the demo source {id}?"
        - "please open demo {id}"
        - "i need the example code for {id}"
        - "view source of demo {id}"
        - "show example source {id}"
        - "open the code for {id}"
        - "where is demo {id} source"
