# Note commands
commands:
  - id: note
    human:
      ru:
        - "запиши {text}"
        - "обнови заметку {text}"
        - "напиши в заметку {text}"
        - "установи текст заметки {text}"
        - "заметка {text}"
        - "вставь в заметку {text}"
        - "можно записать {text}?"
        - "пожалуйста напиши {text}"
        - "мне нужно сохранить {text} в заметке"
        - "задай заметке текст {text}"
        - "write {text}"
      en:
        - "set note to {text}"
        - "write {text}"
        - "update note with {text}"
        - "note {text}"
        - "put {text} in the note"
        - "can you write {text}?"
        - "please write {text}"
        - "i need the note to say {text}"
        - "set the note text to {text}"
        - "save {text} in the note"
        - "replace note with {text}"

  - id: note:add
    human:
      ru:
        - "допиши {text}"
        - "добавь в заметку {text}"
        - "припиши {text}"
        - "дополни заметку {text}"
        - "добавь текст {text}"
        - "докинь в заметку {text}"
        - "можно дописать {text}?"
        - "пожалуйста добавь {text} в заметку"
        - "мне нужно дописать {text}"
        - "append {text} to note"
        - "внеси {text} в заметку"
      en:
        - "append {text} to note"
        - "add {text} to the note"
        - "add to note {text}"
        - "append note with {text}"
        - "can you add {text} to the note?"
        - "please append {text}"
        - "i need to add {text}"
        - "put {text} at the end of the note"
        - "extend the note with {text}"
        - "add text {text}"
        - "write more: {text}"

  - id: note:create
    human:
      ru:
        - "создай заметку {title}"
        - "добавь заметку {title}"
        - "новая заметка {title}"
        - "сделай заметку {title}"
        - "запиши заметку {title}"
        - "создать заметку {title}"
        - "сделай новую заметку {title}"
        - "можно создать заметку {title}?"
        - "пожалуйста создай заметку {title}"
        - "мне нужна заметка {title}"
        - "хочу заметку {title}"
      en:
        - "create note {title}"
        - "add a note {title}"
        - "new note {title}"
        - "make a note {title}"
        - "write a note {title}"
        - "create a new note {title}"
        - "can you create note {title}?"
        - "please create note {title}"
        - "i need a note {title}"
        - "set up note {title}"
        - "make note {title}"

  - id: note:append
    human:
      ru:
        - "допиши к заметке {panel} текст {text}"
        - "добавь в {panel} {text}"
        - "дополни заметку {panel} текстом {text}"
        - "припиши в {panel} {text}"
        - "можно добавить {text} в {panel}?"
        - "пожалуйста допиши {text} в {panel}"
        - "мне нужно внести {text} в {panel}"
        - "append {text} to note {panel}"
        - "добавь текст {text} в заметку {panel}"
        - "закинь в {panel} {text}"
        - "обнови {panel} текстом {text}"
      en:
        - "append {text} to note {panel}"
        - "add {text} to {panel}"
        - "append {panel} with {text}"
        - "add text {text} to note {panel}"
        - "can you add {text} to {panel}?"
        - "please append {text} to {panel}"
        - "i need to add {text} to {panel}"
        - "put {text} into {panel}"
        - "extend note {panel} with {text}"
        - "update {panel} with {text}"
        - "write {text} in note {panel}"

  - id: note:wrap
    human:
      ru:
        - "включи автовысоту заметки {panel}"
        - "авторазмер заметки {panel}"
        - "включи перенос для заметки {panel}"
        - "сделай заметку {panel} с переносом"
        - "можно включить wrap для {panel}?"
        - "пожалуйста включи wrap у {panel}"
        - "мне нужен авторазмер для {panel}"
        - "разреши перенос текста в {panel}"
        - "note wrap {panel}"
        - "автовысота для {panel}"
        - "включи wrap {panel}"
      en:
        - "enable note wrap {panel}"
        - "auto height for note {panel}"
        - "turn on wrap for note {panel}"
        - "make note {panel} wrapped"
        - "can you enable wrap for {panel}?"
        - "please turn on wrap for {panel}"
        - "i need auto height on {panel}"
        - "allow text wrapping in {panel}"
        - "note wrap {panel}"
        - "enable auto height {panel}"
        - "wrap note {panel}"

  - id: note:nowrap
    human:
      ru:
        - "выключи автовысоту заметки {panel}"
        - "фиксированный размер заметки {panel}"
        - "выключи перенос у заметки {panel}"
        - "сделай заметку {panel} без переноса"
        - "можно отключить wrap для {panel}?"
        - "пожалуйста выключи wrap у {panel}"
        - "мне нужен фиксированный размер для {panel}"
        - "запрети перенос текста в {panel}"
        - "disable note wrap {panel}"
        - "фикс высота {panel}"
        - "выключи wrap {panel}"
      en:
        - "disable note wrap {panel}"
        - "fixed height for note {panel}"
        - "turn off wrap for note {panel}"
        - "make note {panel} not wrap"
        - "can you disable wrap for {panel}?"
        - "please turn off wrap for {panel}"
        - "i need fixed height on {panel}"
        - "prevent text wrapping in {panel}"
        - "note nowrap {panel}"
        - "disable auto height {panel}"
        - "unwrap note {panel}"
