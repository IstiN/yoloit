# Checklist commands
commands:
  - id: checklist:add
    human:
      ru:
        - "добавь в чеклист {item}"
        - "добавь пункт {item}"
        - "запиши в список {item}"
        - "добавь задачу {item}"
        - "добавь в список {item}"
        - "новый пункт {item}"
        - "внеси пункт {item}"
        - "можно добавить {item} в чеклист?"
        - "пожалуйста добавь {item}"
        - "мне нужен пункт {item}"
        - "закинь в чеклист {item}"
      en:
        - "add {item} to checklist"
        - "add checklist item {item}"
        - "add task {item}"
        - "add to list {item}"
        - "new item {item}"
        - "put {item} on the checklist"
        - "can you add {item} to the checklist?"
        - "please add {item}"
        - "i need checklist item {item}"
        - "insert item {item}"
        - "append checklist with {item}"

  - id: checklist:new
    human:
      ru:
        - "создай чеклист {title}"
        - "новый чеклист {title}"
        - "создай список {title}"
        - "добавь чеклист {title}"
        - "создать список задач {title}"
        - "сделай чеклист {title}"
        - "можно создать чеклист {title}?"
        - "пожалуйста создай чеклист {title}"
        - "мне нужен чеклист {title}"
        - "хочу список {title}"
        - "create checklist {title}"
      en:
        - "create checklist {title}"
        - "new checklist {title}"
        - "add checklist {title}"
        - "make checklist {title}"
        - "create todo list {title}"
        - "make a checklist called {title}"
        - "can you create checklist {title}?"
        - "please create checklist {title}"
        - "i need a checklist {title}"
        - "set up checklist {title}"
        - "start checklist {title}"

  - id: checklist:check
    human:
      ru:
        - "отметь {item}"
        - "выполни {item}"
        - "сделано {item}"
        - "отметь как сделано {item}"
        - "галочка {item}"
        - "чек {item}"
        - "закрой пункт {item}"
        - "можно отметить {item}?"
        - "пожалуйста отметь {item} как выполнено"
        - "мне нужно зачекать {item}"
        - "пометь {item} выполненным"
      en:
        - "check {item}"
        - "mark {item} done"
        - "complete {item}"
        - "tick {item}"
        - "done {item}"
        - "mark {item} as completed"
        - "can you check {item}?"
        - "please mark {item} done"
        - "i need to complete {item}"
        - "check off {item}"
        - "set {item} to done"

  - id: checklist:uncheck
    human:
      ru:
        - "убери отметку {item}"
        - "сними галочку {item}"
        - "не сделано {item}"
        - "анчек {item}"
        - "сделай {item} невыполненным"
        - "можно снять отметку с {item}?"
        - "пожалуйста разчекни {item}"
        - "мне нужно вернуть {item} в список"
        - "отмени выполнение {item}"
        - "убери done с {item}"
        - "верни пункт {item}"
      en:
        - "uncheck {item}"
        - "mark {item} not done"
        - "untick {item}"
        - "undo {item}"
        - "mark {item} as incomplete"
        - "can you uncheck {item}?"
        - "please mark {item} not done"
        - "i need to reopen {item}"
        - "clear the check on {item}"
        - "set {item} back to todo"
        - "unmark {item}"
