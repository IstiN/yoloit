# Panel management commands
commands:
  - id: panels
    human:
      ru:
        - "покажи панели"
        - "список панелей"
        - "какие панели есть"
        - "что на борде"
        - "все панели"
        - "покажи все панели"
        - "можно список панелей?"
        - "какие панели на доске"
        - "мне нужны панели"
        - "открой список панелей"
        - "панели"
      en:
        - "show panels"
        - "list panels"
        - "what panels are there"
        - "what is on the board"
        - "all panels"
        - "show all panels"
        - "can i see the panels?"
        - "which panels are on the board"
        - "i need the panel list"
        - "open panel list"
        - "panels"

  - id: panel
    human:
      ru:
        - "покажи панель {panel}"
        - "детали панели {panel}"
        - "содержимое панели {panel}"
        - "что в панели {panel}"
        - "открой панель {panel}"
        - "можно посмотреть панель {panel}?"
        - "пожалуйста покажи панель {panel}"
        - "мне нужна панель {panel}"
        - "информация о панели {panel}"
        - "раскрой панель {panel}"
        - "show panel {panel}"
      en:
        - "show panel {panel}"
        - "panel details {panel}"
        - "panel content {panel}"
        - "what is in panel {panel}"
        - "open panel {panel}"
        - "can i see panel {panel}?"
        - "please show panel {panel}"
        - "i need panel {panel}"
        - "panel info {panel}"
        - "display panel {panel}"
        - "reveal panel {panel}"

  - id: panel:create
    human:
      ru:
        - "создай панель {title}"
        - "добавь панель {title}"
        - "новая панель {title}"
        - "сделай панель {title}"
        - "создай виджет {title}"
        - "можно создать панель {title}?"
        - "пожалуйста добавь панель {title}"
        - "мне нужна панель {title}"
        - "организуй панель {title}"
        - "хочу новую панель {title}"
        - "создать панель {title}"
      en:
        - "create panel {title}"
        - "add panel {title}"
        - "new panel {title}"
        - "make panel {title}"
        - "create widget {title}"
        - "can you create panel {title}?"
        - "please add panel {title}"
        - "i need a panel {title}"
        - "set up panel {title}"
        - "make a new panel {title}"
        - "build panel {title}"

  - id: panel:delete
    human:
      ru:
        - "удали панель {panel}"
        - "убери панель {panel}"
        - "удали {panel}"
        - "убери {panel}"
        - "удали все заметки"
        - "удали заметку {panel}"
        - "удали виджет {panel}"
        - "удали чат {panel}"
        - "удалить панель {panel}"
        - "можно удалить панель {panel}?"
        - "пожалуйста убери {panel}"
      en:
        - "delete panel {panel}"
        - "remove panel {panel}"
        - "delete {panel}"
        - "remove {panel}"
        - "delete all notes"
        - "delete note {panel}"
        - "remove widget {panel}"
        - "delete chat {panel}"
        - "can you delete panel {panel}?"
        - "please remove {panel}"
        - "get rid of panel {panel}"

  - id: panel:rename
    human:
      ru:
        - "переименуй панель {panel} в {new_title}"
        - "назови панель {panel} {new_title}"
        - "переименуй {panel} в {new_title}"
        - "измени название {panel} на {new_title}"
        - "смени имя панели {panel} на {new_title}"
        - "можно переименовать {panel} в {new_title}?"
        - "пожалуйста назови панель {panel} как {new_title}"
        - "хочу назвать {panel} как {new_title}"
        - "задай заголовок {new_title} для {panel}"
        - "rename panel {panel} to {new_title}"
        - "сделай {new_title} названием панели {panel}"
      en:
        - "rename panel {panel} to {new_title}"
        - "rename {panel} to {new_title}"
        - "call panel {panel} {new_title}"
        - "change panel name {panel} to {new_title}"
        - "set panel {panel} title to {new_title}"
        - "can you rename panel {panel} to {new_title}?"
        - "please rename {panel} to {new_title}"
        - "i want {panel} named {new_title}"
        - "update panel title {panel} to {new_title}"
        - "make panel {panel} called {new_title}"
        - "change the title of {panel} to {new_title}"

  - id: panel:move
    human:
      ru:
        - "подвинь панель {panel} на {x} {y}"
        - "перемести {panel} на {x} {y}"
        - "сдвинь {panel} на {x} {y}"
        - "поставь панель {panel} в {x} {y}"
        - "можно переместить {panel} на {x} {y}?"
        - "пожалуйста передвинь {panel} на {x} {y}"
        - "мне нужно сместить {panel} в {x} {y}"
        - "перетащи {panel} на {x} {y}"
        - "move panel {panel} to {x} {y}"
        - "подай панель {panel} в {x} {y}"
        - "расположи {panel} на {x} {y}"
      en:
        - "move panel {panel} to {x} {y}"
        - "move {panel} to {x} {y}"
        - "position {panel} at {x} {y}"
        - "place panel {panel} at {x} {y}"
        - "can you move {panel} to {x} {y}?"
        - "please move panel {panel} to {x} {y}"
        - "i need {panel} at {x} {y}"
        - "drag {panel} to {x} {y}"
        - "shift panel {panel} to {x} {y}"
        - "set the position of {panel} to {x} {y}"
        - "put {panel} at {x} {y}"

  - id: panel:resize
    human:
      ru:
        - "измени размер панели {panel} на {width} {height}"
        - "установи размер {panel} {width} на {height}"
        - "сделай {panel} размером {width} на {height}"
        - "ресайз {panel} {width} {height}"
        - "можно изменить размер {panel} на {width} {height}?"
        - "пожалуйста задай размер {panel} {width} {height}"
        - "мне нужен размер {width} {height} для {panel}"
        - "растяни {panel} до {width} {height}"
        - "сожми {panel} до {width} {height}"
        - "resize panel {panel} to {width} {height}"
        - "выставь габариты {panel} {width} {height}"
      en:
        - "resize panel {panel} to {width} {height}"
        - "resize {panel} to {width} by {height}"
        - "set size of {panel} to {width} {height}"
        - "make panel {panel} {width} {height}"
        - "can you resize {panel} to {width} {height}?"
        - "please set {panel} to {width} {height}"
        - "i need {panel} sized {width} {height}"
        - "stretch {panel} to {width} {height}"
        - "shrink {panel} to {width} {height}"
        - "change dimensions of {panel} to {width} {height}"
        - "panel size {panel} {width} {height}"

  - id: panel:focus
    human:
      ru:
        - "открой панель {panel}"
        - "покажи панель {panel}"
        - "перейди к панели {panel}"
        - "фокус на {panel}"
        - "сфокусируйся на панели {panel}"
        - "можно открыть панель {panel}?"
        - "пожалуйста покажи {panel}"
        - "мне нужна панель {panel} на экране"
        - "активируй панель {panel}"
        - "зафокусить {panel}"
        - "go to panel {panel}"
      en:
        - "focus panel {panel}"
        - "open panel {panel}"
        - "show panel {panel}"
        - "go to panel {panel}"
        - "bring up panel {panel}"
        - "can you focus panel {panel}?"
        - "please open {panel}"
        - "i want panel {panel}"
        - "activate panel {panel}"
        - "take me to panel {panel}"
        - "jump to panel {panel}"

  - id: panel:color
    human:
      ru:
        - "покрась панель {panel} в {color}"
        - "цвет панели {panel} {color}"
        - "установи цвет {panel} {color}"
        - "сделай {panel} {color}"
        - "перекрась панель {panel} в {color}"
        - "можно задать цвет {color} для {panel}?"
        - "пожалуйста покрась {panel} в {color}"
        - "мне нужен цвет {color} для {panel}"
        - "change color of {panel} to {color}"
        - "сделай цвет панели {panel} {color}"
        - "задай {color} для панели {panel}"
      en:
        - "color panel {panel} {color}"
        - "set panel {panel} color to {color}"
        - "paint {panel} {color}"
        - "make {panel} {color}"
        - "change panel {panel} to {color}"
        - "can you color {panel} {color}?"
        - "please set {panel} to {color}"
        - "i need panel {panel} in {color}"
        - "recolor panel {panel} {color}"
        - "set the color of {panel} to {color}"
        - "panel {panel} color {color}"

  - id: panel:hide
    human:
      ru:
        - "спрячь панель {panel}"
        - "скрой {panel}"
        - "убери {panel} с экрана"
        - "скрой панель {panel}"
        - "можно спрятать {panel}?"
        - "пожалуйста спрячь {panel}"
        - "мне не нужна панель {panel} на экране"
        - "закрой видимость {panel}"
        - "hide panel {panel}"
        - "замаскируй {panel}"
        - "убери из вида {panel}"
      en:
        - "hide panel {panel}"
        - "hide {panel}"
        - "conceal {panel}"
        - "hide the panel {panel}"
        - "can you hide {panel}?"
        - "please hide panel {panel}"
        - "i don't want to see {panel}"
        - "make {panel} invisible"
        - "close visibility for {panel}"
        - "take {panel} off screen"
        - "stash panel {panel}"

  - id: panel:show
    human:
      ru:
        - "покажи скрытую панель {panel}"
        - "верни панель {panel}"
        - "отобрази {panel}"
        - "раскрой панель {panel}"
        - "можно показать {panel}?"
        - "пожалуйста верни {panel}"
        - "мне снова нужна панель {panel}"
        - "сделай {panel} видимой"
        - "show panel {panel}"
        - "unhide {panel}"
        - "покажи {panel} обратно"
      en:
        - "show hidden panel {panel}"
        - "unhide panel {panel}"
        - "reveal {panel}"
        - "show panel {panel}"
        - "can you unhide {panel}?"
        - "please bring back {panel}"
        - "i need panel {panel} again"
        - "make {panel} visible"
        - "restore panel {panel}"
        - "show {panel} again"
        - "bring panel {panel} back"

  - id: panel:types
    human:
      ru:
        - "типы панелей"
        - "какие типы панелей есть"
        - "доступные типы"
        - "покажи типы панелей"
        - "какие панели можно создать"
        - "можно список типов панелей?"
        - "пожалуйста покажи доступные виды панелей"
        - "мне нужны типы виджетов"
        - "виды панелей"
        - "какие есть виды панелей"
        - "panel types"
      en:
        - "panel types"
        - "what panel types are available"
        - "available types"
        - "show panel types"
        - "what panels can i create"
        - "can i get the panel type list?"
        - "please show available panel kinds"
        - "i need widget types"
        - "panel kinds"
        - "which panel types exist"
        - "list panel types"

  - id: panel:help
    human:
      ru:
        - "помощь по панели {panel}"
        - "что умеет панель {panel}"
        - "действия панели {panel}"
        - "покажи help для {panel}"
        - "как использовать панель {panel}"
        - "можно помощь по {panel}?"
        - "пожалуйста подскажи по панели {panel}"
        - "мне нужна справка по {panel}"
        - "что можно сделать с {panel}"
        - "panel help {panel}"
        - "инструкция для панели {panel}"
      en:
        - "panel help {panel}"
        - "what can panel {panel} do"
        - "panel actions {panel}"
        - "show help for panel {panel}"
        - "how do i use panel {panel}"
        - "can you help with panel {panel}?"
        - "please show panel {panel} help"
        - "i need help for {panel}"
        - "what can i do with {panel}"
        - "panel guide {panel}"
        - "instructions for panel {panel}"

  - id: do
    human:
      ru:
        - "выполни действие {action} на {panel}"
        - "сделай {action} с {panel}"
        - "запусти {action} на {panel}"
        - "примени {action} к {panel}"
        - "можно выполнить {action} на {panel}?"
        - "пожалуйста сделай {action} с {panel}"
        - "мне нужно действие {action} для {panel}"
        - "триггерни {action} на {panel}"
        - "активируй {action} у {panel}"
        - "do {action} on {panel}"
        - "вызови {action} для {panel}"
      en:
        - "do {action} on {panel}"
        - "execute {action} on {panel}"
        - "run {action} on {panel}"
        - "apply {action} to {panel}"
        - "can you do {action} on {panel}?"
        - "please execute {action} on {panel}"
        - "i need {action} for {panel}"
        - "trigger {action} on {panel}"
        - "activate {action} on {panel}"
        - "perform {action} on {panel}"
        - "invoke {action} for {panel}"
