# Board management commands
commands:
  - id: boards
    human:
      ru:
        - "покажи борды"
        - "список бордов"
        - "какие борды есть"
        - "мои доски"
        - "все борды"
        - "покажи все доски"
        - "можно список бордов?"
        - "какие у меня борды"
        - "мне нужны борды"
        - "открой список досок"
        - "борды"
      en:
        - "show boards"
        - "list boards"
        - "what boards do I have"
        - "my boards"
        - "all boards"
        - "show all boards"
        - "can i see the boards?"
        - "which boards do i have"
        - "i need the board list"
        - "open board list"
        - "boards"

  - id: board
    human:
      ru:
        - "детали борда {board}"
        - "информация о борде {board}"
        - "что на борде {board}"
        - "можно посмотреть борд {board}?"
        - "пожалуйста покажи доску {board}"
        - "содержимое борда {board}"
        - "расскажи про борд {board}"
        - "покажи доску {board}"
        - "подробности борда {board}"
        - "инфо о борде {board}"
      en:
        - "board details {board}"
        - "info about board {board}"
        - "what is on board {board}"
        - "can i see board {board}?"
        - "please show board details {board}"
        - "board info {board}"
        - "tell me about board {board}"
        - "display board info {board}"
        - "board summary {board}"
        - "describe board {board}"

  - id: board:create
    human:
      ru:
        - "создай борд {name}"
        - "создай доску {name}"
        - "новый борд {name}"
        - "новая доска {name}"
        - "добавь борд {name}"
        - "сделай борд {name}"
        - "можно создать борд {name}?"
        - "пожалуйста создай доску {name}"
        - "мне нужен новый борд {name}"
        - "организуй борд {name}"
        - "создать борд {name}"
      en:
        - "create board {name}"
        - "new board {name}"
        - "add board {name}"
        - "make a board called {name}"
        - "make board {name}"
        - "can you create board {name}?"
        - "please create board {name}"
        - "i need a new board {name}"
        - "set up board {name}"
        - "create a board named {name}"
        - "build board {name}"

  - id: board:rename
    human:
      ru:
        - "переименуй борд {board} в {new_name}"
        - "назови борд {board} {new_name}"
        - "измени имя борда {board} на {new_name}"
        - "переименуй доску {board} в {new_name}"
        - "смени название {board} на {new_name}"
        - "можно переименовать борд {board} в {new_name}?"
        - "пожалуйста назови борд {board} как {new_name}"
        - "хочу борд {board} назвать {new_name}"
        - "задай имя {new_name} для борда {board}"
        - "rename board {board} to {new_name}"
        - "сделай имя борда {board} = {new_name}"
      en:
        - "rename board {board} to {new_name}"
        - "change board name {board} to {new_name}"
        - "call board {board} {new_name}"
        - "rename {board} to {new_name}"
        - "set board {board} name to {new_name}"
        - "can you rename board {board} to {new_name}?"
        - "please rename board {board} to {new_name}"
        - "i want board {board} to be {new_name}"
        - "update board title {board} to {new_name}"
        - "change the board {board} to {new_name}"
        - "make board {board} named {new_name}"

  - id: board:delete
    human:
      ru:
        - "удали борд {board}"
        - "удали доску {board}"
        - "убери борд {board}"
        - "удалить борд {board}"
        - "снеси борд {board}"
        - "можно удалить борд {board}?"
        - "пожалуйста убери доску {board}"
        - "мне не нужен борд {board}"
        - "удали board {board}"
        - "выкинь борд {board}"
        - "remove board {board}"
      en:
        - "delete board {board}"
        - "remove board {board}"
        - "destroy board {board}"
        - "delete {board}"
        - "can you remove board {board}?"
        - "please delete board {board}"
        - "i don't need board {board} anymore"
        - "drop board {board}"
        - "get rid of board {board}"
        - "erase board {board}"
        - "trash board {board}"

  - id: board:focus
    human:
      ru:
        - "открой борд {board}"
        - "переключись на {board}"
        - "перейди на {board}"
        - "открой доску {board}"
        - "сфокусируйся на борде {board}"
        - "можно открыть борд {board}?"
        - "пожалуйста перейди на борд {board}"
        - "мне нужен борд {board} на экране"
        - "зафокусить борд {board}"
        - "покажи борд {board}"
      en:
        - "open board {board}"
        - "switch to board {board}"
        - "go to board {board}"
        - "show board {board}"
        - "focus board {board}"
        - "bring up board {board}"
        - "can you open board {board}?"
        - "please switch to board {board}"
        - "i want board {board}"
        - "move to board {board}"
        - "take me to board {board}"

  - id: board:use
    human:
      ru:
        - "используй борд {board}"
        - "выбери борд {board}"
        - "установи борд {board}"
        - "выбери борд {board} как основной"
        - "работай с бордом {board}"
        - "переключись на борд {board}"
        - "сменить борд на {board}"
        - "мой борд {board}"
        - "сделай {board} активным бордом"
        - "использовать доску {board}"
      en:
        - "use board {board}"
        - "set board {board}"
        - "select board {board}"
        - "work with board {board}"
        - "change board to {board}"
        - "set active board {board}"
        - "make {board} the current board"
        - "use {board} as the board"
        - "set {board} as active"
        - "work on board {board}"

  - id: board:current
    human:
      ru:
        - "какой борд сейчас"
        - "текущий борд"
        - "на каком борде я"
        - "какая доска активна"
        - "какой сейчас активный борд"
        - "можно узнать текущий борд?"
        - "покажи текущую доску"
        - "мне нужен активный борд"
        - "что за борд открыт"
        - "current board"
        - "где я сейчас"
      en:
        - "what board am i on"
        - "current board"
        - "which board is active"
        - "what is the active board"
        - "which board is current"
        - "can you show the current board?"
        - "tell me the active board"
        - "what board is open"
        - "i need the current board"
        - "show active board"
        - "where am i right now"

  - id: board:snapshot
    human:
      ru:
        - "снепшот борда {board}"
        - "текстовый снимок борда {board}"
        - "структура борда {board}"
        - "сделай snapshot борда {board}"
        - "покажи снимок борда {board}"
        - "можно snapshot для {board}?"
        - "пожалуйста сохрани снимок борда {board}"
        - "мне нужен снапшот {board}"
        - "зафиксируй состояние борда {board}"
        - "сними структуру {board}"
        - "board snapshot {board}"
      en:
        - "snapshot of board {board}"
        - "board snapshot {board}"
        - "board structure {board}"
        - "take a snapshot of board {board}"
        - "capture board {board} state"
        - "can you snapshot board {board}?"
        - "please save a snapshot of {board}"
        - "i need a snapshot of {board}"
        - "record board {board}"
        - "grab board snapshot {board}"
        - "save board state {board}"

  - id: board:screenshot
    human:
      ru:
        - "скриншот борда {board}"
        - "сфоткай борд {board}"
        - "сохрани скриншот борда {board}"
        - "сделай скрин {board}"
        - "можно скриншот борда {board}?"
        - "пожалуйста сфотографируй доску {board}"
        - "мне нужен скрин борда {board}"
        - "захвати экран борда {board}"
        - "сделай screenshot {board}"
        - "сними борд {board}"
        - "capture board {board}"
      en:
        - "screenshot board {board}"
        - "capture board {board}"
        - "save screenshot of board {board}"
        - "take a screenshot of board {board}"
        - "grab a screenshot for {board}"
        - "can you screenshot board {board}?"
        - "please capture board {board}"
        - "i need a screenshot of {board}"
        - "save a board shot for {board}"
        - "screen capture {board}"
        - "take board image {board}"

  - id: board:arrange
    human:
      ru:
        - "расставь панели"
        - "упорядочи панели"
        - "выровняй панели"
        - "автораскладка"
        - "авторасставь панели"
        - "наведи порядок на борде"
        - "можно упорядочить панели?"
        - "пожалуйста разложи панели"
        - "мне нужно аккуратно разложить панели"
        - "организуй панели"
        - "arrange panels"
      en:
        - "arrange panels"
        - "organize panels"
        - "auto arrange"
        - "tidy up panels"
        - "auto layout panels"
        - "clean up the board"
        - "can you arrange the panels?"
        - "please organize the panels"
        - "i need the panels aligned"
        - "lay out the panels"
        - "straighten the panels"

  - id: board:fit
    human:
      ru:
        - "покажи весь борд"
        - "уместить все"
        - "зумируй чтобы все влезло"
        - "подгони под экран"
        - "покажи весь холст"
        - "можно уместить весь борд?"
        - "пожалуйста fit to screen"
        - "мне нужно видеть все"
        - "зум до всего содержимого"
        - "вписать борд в экран"
        - "fit board"
      en:
        - "fit to view"
        - "show everything"
        - "zoom to fit"
        - "fit board to screen"
        - "show the whole board"
        - "can you fit everything?"
        - "please zoom to fit"
        - "i need to see it all"
        - "fit all content"
        - "frame the whole board"
        - "fit board"

  - id: board:zoom
    human:
      ru:
        - "зум {scale}"
        - "масштаб {scale}"
        - "увеличь до {scale}"
        - "установи зум {scale}"
        - "сделай масштаб {scale}"
        - "можно zoom {scale}?"
        - "пожалуйста поставь масштаб {scale}"
        - "мне нужен зум {scale}"
        - "приблизь до {scale}"
        - "отдалить до {scale}"
        - "zoom {scale}"
      en:
        - "zoom {scale}"
        - "set zoom {scale}"
        - "scale to {scale}"
        - "set board zoom to {scale}"
        - "make zoom {scale}"
        - "can you zoom to {scale}?"
        - "please set zoom to {scale}"
        - "i need zoom {scale}"
        - "adjust scale to {scale}"
        - "change zoom to {scale}"
        - "board zoom {scale}"

  - id: board:diagram
    human:
      ru:
        - "диаграмма борда {board}"
        - "нарисуй схему борда {board}"
        - "покажи диаграмму {board}"
        - "схема доски {board}"
        - "можно диаграмму борда {board}?"
        - "пожалуйста построй схему {board}"
        - "мне нужна диаграмма {board}"
        - "визуализируй борд {board}"
        - "сделай diagram для {board}"
        - "нарисуй карту борда {board}"
        - "board diagram {board}"
      en:
        - "board diagram {board}"
        - "draw board diagram {board}"
        - "show diagram for board {board}"
        - "map board {board}"
        - "can you diagram board {board}?"
        - "please build a diagram of {board}"
        - "i need a diagram of {board}"
        - "visualize board {board}"
        - "render board map {board}"
        - "draw a scheme for {board}"
        - "board map {board}"

  - id: board:svg
    human:
      ru:
        - "экспорт SVG борда {board}"
        - "сохрани борд {board} как SVG"
        - "выгрузи {board} в SVG"
        - "сделай svg для борда {board}"
        - "можно экспортировать борд {board} в SVG?"
        - "пожалуйста сохрани {board} svg"
        - "мне нужен SVG борда {board}"
        - "экспортни борд {board}"
        - "создай svg-версию {board}"
        - "board {board} в svg"
        - "export board {board} as SVG"
      en:
        - "export board {board} as SVG"
        - "save board {board} SVG"
        - "export {board} to SVG"
        - "make an SVG of board {board}"
        - "can you export board {board} as SVG?"
        - "please save {board} as svg"
        - "i need an SVG for {board}"
        - "generate svg for board {board}"
        - "create svg version of {board}"
        - "output board {board} as svg"
        - "board svg {board}"

  - id: board:apply
    human:
      ru:
        - "примени YAML {file}"
        - "загрузи конфигурацию {file}"
        - "примени файл {file}"
        - "импортируй yaml {file}"
        - "можно применить {file}?"
        - "пожалуйста загрузи {file}"
        - "мне нужно применить конфиг {file}"
        - "подтяни board yaml {file}"
        - "используй файл {file}"
        - "apply yaml {file}"
        - "загрузи board config {file}"
      en:
        - "apply YAML {file}"
        - "load config {file}"
        - "apply file {file}"
        - "import yaml {file}"
        - "can you apply {file}?"
        - "please load {file}"
        - "i need to apply config {file}"
        - "use file {file}"
        - "load board config {file}"
        - "read yaml {file}"
        - "apply board yaml {file}"

  - id: board:translate
    human:
      ru:
        - "сдвинь борд на {x} {y}"
        - "подвинь вид на {x} {y}"
        - "перемести холст на {x} {y}"
        - "сдвинь viewport на {x} {y}"
        - "можно сместить борд на {x} {y}?"
        - "пожалуйста подвинь борд на {x} {y}"
        - "мне нужно смещение {x} {y}"
        - "панорамируй на {x} {y}"
        - "перетащи вид на {x} {y}"
        - "translate board {x} {y}"
        - "смести доску на {x} {y}"
      en:
        - "move viewport to {x} {y}"
        - "translate board {x} {y}"
        - "pan to {x} {y}"
        - "move the board by {x} {y}"
        - "shift viewport {x} {y}"
        - "can you move the view to {x} {y}?"
        - "please pan the board to {x} {y}"
        - "i need an offset of {x} {y}"
        - "drag the view to {x} {y}"
        - "shift the canvas {x} {y}"
        - "move board view {x} {y}"
