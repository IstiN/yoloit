/// Web stub for [fetchJson].
///
/// Returns null so the catalog service falls back to the bundled asset.
Future<Map<String, dynamic>?> fetchJson({
  required String url,
  required Duration timeout,
  Map<String, String>? headers,
  String userAgent = 'YoLoIT',
  dynamic client,
}) async => null;
